-- ---------------------------------------------------------------------------
-- Управление дизайном страниц: включение/отключение блока без удаления.
-- is_active = 0 — блок остаётся в странице (с содержимым и порядком), но не
-- выводится на сайте. Порядок и шаблоны (сниппеты) уже поддержаны ранее.
-- ---------------------------------------------------------------------------
ALTER TABLE blocks
    ADD COLUMN is_active TINYINT(1) NOT NULL DEFAULT 1 AFTER sort_order;

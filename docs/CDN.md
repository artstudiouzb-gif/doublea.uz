# Раздача статики через CDN и инвалидация кэша (задача 127)

Проект отдаёт две категории статики:

- `public/assets/` — CSS/JS/шрифты приложения;
- `public/uploads/public/` — загруженные изображения (и их WebP-варианты).

## Версионирование и cache-busting

URL версионированных ассетов формирует `App\Core\Asset::url()`: к пути
добавляется отпечаток содержимого `?v=<hash>` (из mtime+size файла). При деплое
изменённого файла хэш меняется → URL становится новым ресурсом, и CDN отдаёт
свежую версию **без ручной очистки кэша**. Старые версии можно кэшировать
«навсегда».

Заголовок для этого задаётся в `public/.htaccess`:

```
<FilesMatch "\.(css|js|woff2?|ttf|eot|svg|jpe?g|png|gif|webp|ico)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
</FilesMatch>
```

Загруженные изображения имеют случайные (контентно-уникальные) имена, поэтому
их URL также стабилен и безопасен для «вечного» кэша.

## Рецепт для Cloudflare

1. **DNS/Proxy.** Домен проксируется через Cloudflare (оранжевое облако).
2. **Cache Rules.** Правило: для путей `/assets/*` и `/uploads/public/*` —
   *Cache Eligibility: Eligible for cache*, *Edge TTL: Respect origin* (origin
   уже отдаёт `max-age=31536000, immutable`).
3. **Отдельный CDN-хост (опционально).** Если статика раздаётся с
   `cdn.example.com`, укажите адрес в «Админка → Производительность → Базовый
   URL CDN». Значение хранится как настройка `perf_cdn_url`; `Asset::url()` и
   переписывание публичных загрузок применят его автоматически. Без этой
   настройки используются относительные URL текущего домена.
4. **Инвалидация при деплое.** Не требуется вручную: изменённые файлы получают
   новый `?v=`, а неизменные остаются в кэше. Полная «Purge Everything»
   опциональна для экстренных случаев.

## Рецепт для BunnyCDN (или любого pull-zone CDN) — БЕЗ переноса домена

Подходит, когда домен **нельзя** перевести в Cloudflare (сменить NS). CDN тянет
файлы с origin (этого сайта) и раздаёт со своего хоста — домен трогать не нужно.

1. **Создайте Pull Zone** в BunnyCDN: Origin URL = `https://<ваш-домен>`
   (например `https://artstudio.uz`). Bunny выдаст хост вида
   `yourzone.b-cdn.net`.
2. **Впишите хост** в админке: *Производительность → CDN для статики и
   загрузок → Базовый URL CDN* = `https://yourzone.b-cdn.net`.
   С этого момента `App\Core\Asset::url()` префиксует им `/assets`, а
   `App\Core\View::render()` переписывает `/uploads/public/...` в HTML на тот же
   хост. HTML по-прежнему отдаёт origin — CDN кэширует только статику и медиа.
3. **Инвалидация не нужна:** ассеты версионируются (`?v=`), у загрузок
   уникальные имена. Отдельный API-клиент BunnyCDN не требуется.
4. **(Опционально) свой поддомен** `cdn.example.com` вместо `*.b-cdn.net`:
   добавьте CNAME на хост Bunny — это единственное изменение DNS, и оно не
   затрагивает основной домен.

Аналогично настраиваются KeyCDN, Fastly, AWS CloudFront (origin-pull).

## Очистка кэша Cloudflare по API (когда сайт/поддомен реально за Cloudflare)

Если origin проксируется через Cloudflare (или за ним поддомен, раздающий
статику), включите *Производительность → Cloudflare*: токен (`Zone · Cache
Purge`) + Zone ID. Тогда `App\Core\Cloudflare::purgeSite()` очищает кэш при
изменении контента (хук в `Cache::forgetPrefix('page:')`, не чаще раза за
запрос). Для pull-zone без HTML-кэширования это не требуется (см. выше).

## Проверка

После деплоя новой версии, например, `frontend.css`:

- страница ссылается на `/assets/css/frontend.css?v=<новый hash>`;
- CDN, не найдя такой URL в кэше, запрашивает origin и кэширует новую версию;
- предыдущая версия (`?v=<старый hash>`) остаётся в кэше, но больше не
  запрашивается — конфликтов и «залипшего» кэша нет.

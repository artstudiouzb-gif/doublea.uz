-- Фоновая надпись слайда обложки: крупное полупрозрачное слово за контентом.
-- Это текст, а не оформление, поэтому он переводится наравне с заголовком:
-- на другом языке там будет другое слово, а не то же самое.
ALTER TABLE hero_slide_translations
    ADD COLUMN watermark VARCHAR(120) NULL AFTER art_alt;

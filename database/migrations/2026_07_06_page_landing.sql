-- ---------------------------------------------------------------------------
-- Группа 6 — лендинг-лейаут страницы: флаг «скрыть шапку и футер сайта».
-- Обычные страницы: hide_chrome = 0 (обратная совместимость).
-- ---------------------------------------------------------------------------
ALTER TABLE pages
    ADD COLUMN hide_chrome TINYINT(1) NOT NULL DEFAULT 0 COMMENT 'лендинг: скрыть шапку/футер сайта';
